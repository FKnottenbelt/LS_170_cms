<!DOCTYPE html>
<html>
  <head>
    <title>CMS</title>
    <link rel="stylesheet" href="/stylesheets/application.css">
  </head>
  <body>

    <% if session[:message] %>
      <p class='message'><%= session.delete(:message) %></p>
    <% end %>

    <main>
      <%== yield %>
    </main>
  </body>
</html>