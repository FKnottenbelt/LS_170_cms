<!DOCTYPE html>
<html>
  <head>
    <style>
      .edit {
        display: inline-block;
        padding-left: 10px ;
      }

      label, textarea {
        font-size: .8rem;
        letter-spacing: 1px;
      }
      textarea {
          padding: 10px;
          line-height: 1.5;
          border-radius: 5px;
          border: 1px solid #ccc;
          box-shadow: 1px 1px 1px #999;
      }

      label {
          display: block;
          margin-bottom: 10px;
      }

    </style>
  </head>
  <body>

    <% if session[:message] %>
    <%= session.delete(:message) %>
    <% end %>

    <main>
      <%== yield %>
    </main>
  </body>
</html>